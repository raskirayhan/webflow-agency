# Sheikh Raski Rayhan Portfolio

This portfolio contains a static frontend and a simple backend API for dynamic project content.

## Run the backend

1. Open a terminal in `backend`
2. Run `npm install`
3. Run `npm start`

The backend will be available at `http://localhost:4000`.

## Preview the portfolio

Open `index.html` directly in the browser or serve the folder with a static server.
